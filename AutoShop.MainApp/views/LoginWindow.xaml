<Window x:Class="AutoShop.MainApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="340"
        Width="420"
        MinHeight="340"
        MinWidth="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <StackPanel VerticalAlignment="Center" Margin="0">
            <TextBlock Text="AutoShop Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" />

            <TextBlock Text="Username" />
            <TextBox x:Name="UserNameBox" Height="30" Margin="0,0,0,10" />

            <TextBlock Text="Password" />
            <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,0,0,10" />

            <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,0,0,10" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Login" Width="90" Margin="0,0,8,0" Click="Login_Click" />
                <Button Content="Cancel" Width="90" Click="Cancel_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>