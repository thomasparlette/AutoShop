<UserControl x:Class="AutoShop.MainApp.Views.ShopSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <StackPanel Width="520">
                <TextBlock Text="Shop Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,16" />
                
                <TextBlock Text="Shop Name" />
                <TextBox Text="{Binding ShopName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Address Line 1" />
                <TextBox Text="{Binding AddressLine1, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Address Line 2" />
                <TextBox Text="{Binding AddressLine2, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="City" />
                <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="State" />
                <TextBox Text="{Binding State, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Postal Code" />
                <TextBox Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Phone" />
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Email" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Website" />
                <TextBox Text="{Binding Website, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Tax Rate" />
                <TextBox Text="{Binding TaxRate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Receipt Footer Text" />
                <TextBox Text="{Binding ReceiptFooterText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         Height="80"
                         TextWrapping="Wrap"
                         Margin="0,0,0,8" />

                <TextBlock Text="Default Thank You Message" />
                <TextBox Text="{Binding DefaultThankYouMessage, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         Height="80"
                         TextWrapping="Wrap"
                         Margin="0,0,0,16" />
                <TextBlock Text="Shop Logo" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Browse Logo" Width="110" Margin="0,0,8,0" Command="{Binding BrowseLogoCommand}" />
                    <TextBox Text="{Binding LogoPath, UpdateSourceTrigger=PropertyChanged}" Width="360" />
                </StackPanel>
                <Image Source="{Binding LogoPreview}"
                       Height="120"
                       Margin="0,0,0,16"
                       Stretch="Uniform" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="Save" Width="90" Margin="0,0,8,0" Command="{Binding SaveCommand}" />
                    <Button Content="Reload" Width="90" Command="{Binding ReloadCommand}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>