<UserControl x:Class="AutoShop.MainApp.Views.VehicleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <!-- Left side -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="Vehicles" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="250"
                             Height="30"
                             Margin="0,0,8,0"
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Content="Search" Width="80" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
                    <Button Content="New" Width="80" Command="{Binding NewCommand}" />
                </StackPanel>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Vehicles}"
                      SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="70" />
                    <DataGridTextColumn Header="Make" Binding="{Binding Make}" Width="*" />
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*" />
                    <DataGridTextColumn Header="Plate" Binding="{Binding LicensePlate}" Width="*" />
                    <DataGridTextColumn Header="VIN" Binding="{Binding Vin}" Width="2*" />
                    <DataGridTextColumn Header="Owner" Binding="{Binding Customer.FullName}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Right side -->
        <Border Grid.Column="2" Padding="16" BorderThickness="1" BorderBrush="Gray" CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Vehicle Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

                    <TextBlock Text="Customer" />
                    <ComboBox ItemsSource="{Binding Customers}"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedCustomerId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="FullName"
                              Margin="0,0,0,8" />

                    <TextBlock Text="Year" />
                    <TextBox Text="{Binding CurrentVehicle.Year, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Make" />
                    <TextBox Text="{Binding CurrentVehicle.Make, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Model" />
                    <TextBox Text="{Binding CurrentVehicle.Model, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Trim" />
                    <TextBox Text="{Binding CurrentVehicle.Trim, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="VIN" />
                    <TextBox Text="{Binding CurrentVehicle.Vin, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="License Plate" />
                    <TextBox Text="{Binding CurrentVehicle.LicensePlate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Color" />
                    <TextBox Text="{Binding CurrentVehicle.Color, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Mileage" />
                    <TextBox Text="{Binding CurrentVehicle.Mileage, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Notes" />
                    <TextBox Text="{Binding CurrentVehicle.Notes, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             Height="80"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save" Width="90" Margin="0,0,8,0" Command="{Binding SaveCommand}" />
                        <Button Content="Delete" Width="90" Margin="0,0,8,0" Command="{Binding DeleteCommand}" />
                        <Button Content="Clear" Width="90" Command="{Binding NewCommand}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>