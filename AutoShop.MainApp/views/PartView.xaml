<UserControl x:Class="AutoShop.MainApp.Views.PartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="Inventory" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="220"
                             Height="30"
                             Margin="0,0,8,0"
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Content="Search" Width="80" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
                    <Button Content="New" Width="80" Command="{Binding NewCommand}" />
                </StackPanel>

                <CheckBox Content="Show inactive"
                          IsChecked="{Binding ShowInactive}"
                          Margin="0,0,0,8" />
            </StackPanel>

            <DataGrid ItemsSource="{Binding Parts}"
                      SelectedItem="{Binding SelectedPart, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Part #" Binding="{Binding PartNumber}" Width="*" />
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                    <DataGridTextColumn Header="Qty" Binding="{Binding QuantityOnHand}" Width="70" />
                    <DataGridTextColumn Header="Cost" Binding="{Binding Cost, StringFormat=C}" Width="90" />
                    <DataGridTextColumn Header="Sell" Binding="{Binding SellPrice, StringFormat=C}" Width="90" />
                    <DataGridTextColumn Header="Reorder" Binding="{Binding ReorderLevel}" Width="80" />
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding Active}" Width="70" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <Border Grid.Column="2" Padding="16" BorderThickness="1" BorderBrush="Gray" CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Part Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

                    <TextBlock Text="Part Number" />
                    <TextBox Text="{Binding CurrentPart.PartNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Description" />
                    <TextBox Text="{Binding CurrentPart.Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Supplier" />
                    <TextBox Text="{Binding CurrentPart.Supplier, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Cost" />
                    <TextBox Text="{Binding CostText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Sell Price" />
                    <TextBox Text="{Binding SellPriceText, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True"
                             Margin="0,0,0,8" />

                    <TextBlock Text="Quantity On Hand" />
                    <TextBox Text="{Binding CurrentPart.QuantityOnHand, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="Reorder Level" />
                    <TextBox Text="{Binding CurrentPart.ReorderLevel, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <CheckBox Content="Active"
                              IsChecked="{Binding CurrentPart.Active}"
                              Margin="0,0,0,12" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save" Width="90" Margin="0,0,8,0" Command="{Binding SaveCommand}" />
                        <Button Content="Delete" Width="90" Margin="0,0,8,0" Command="{Binding DeleteCommand}" />
                        <Button Content="Clear" Width="90" Command="{Binding NewCommand}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>