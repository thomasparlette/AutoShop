<UserControl x:Class="AutoShop.MainApp.Views.WorkOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1.4*" />
        </Grid.ColumnDefinitions>

        <!-- Left side -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <TextBlock Text="Work Orders" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBox Width="250"
                             Height="30"
                             Margin="0,0,8,0"
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Content="Search" Width="80" Margin="0,0,8,0" Command="{Binding RefreshCommand}" />
                    <Button Content="New" Width="80" Command="{Binding NewCommand}" />
                </StackPanel>
            </StackPanel>

            <DataGrid ItemsSource="{Binding WorkOrders}"
                      SelectedItem="{Binding SelectedWorkOrder, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="WO #" Binding="{Binding WorkOrderNumber}" Width="*" />
                    <DataGridTextColumn Header="Customer" Binding="{Binding Customer.FullName}" Width="2*" />
                    <DataGridTextColumn Header="Vehicle" Binding="{Binding Vehicle.Make}" Width="*" />
                    <DataGridTextColumn Header="Model" Binding="{Binding Vehicle.Model}" Width="*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                    <DataGridTextColumn Header="Total" Binding="{Binding GrandTotal}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Right side -->
        <Border Grid.Column="2" Padding="16" BorderThickness="1" BorderBrush="Gray" CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Work Order Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

                    <TextBlock Text="Customer" />
                    <ComboBox ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                              DisplayMemberPath="FullName"
                              Margin="0,0,0,8" />

                    <TextBlock Text="Vehicle" />
                    <ComboBox ItemsSource="{Binding Vehicles}"
                              SelectedItem="{Binding SelectedVehicle, Mode=TwoWay}"
                              DisplayMemberPath="Model"
                              Margin="0,0,0,8" />

                    <TextBlock Text="Status" />
                  <ComboBox ItemsSource="{Binding StatusOptions}"
                            SelectedItem="{Binding SelectedStatus, Mode=TwoWay}"
                            Margin="0,0,0,8" />

                    <TextBlock Text="Complaint" />
                    <TextBox Text="{Binding CurrentWorkOrder.Complaint, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             Height="60"
                             TextWrapping="Wrap"
                             Margin="0,0,0,8" />

                    <TextBlock Text="Diagnosis" />
                    <TextBox Text="{Binding CurrentWorkOrder.Diagnosis, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             Height="60"
                             TextWrapping="Wrap"
                             Margin="0,0,0,8" />

                    <TextBlock Text="Notes" />
                    <TextBox Text="{Binding CurrentWorkOrder.Notes, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             Height="60"
                             TextWrapping="Wrap"
                             Margin="0,0,0,8" />

                    <TextBlock Text="Line Items" FontWeight="Bold" Margin="0,10,0,6" />

                    <DataGrid ItemsSource="{Binding LineItems}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="False"
          Height="180"
          Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type"
                            Binding="{Binding ItemType, Mode=TwoWay}"
                            Width="90" />
                            <DataGridTextColumn Header="Description"
                            Binding="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Width="2*" />
                            <DataGridTextColumn Header="Qty"
                            Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"
                            Width="80" />
                            <DataGridTextColumn Header="Unit Price"
                            Binding="{Binding UnitPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=C}"
                            Width="100" />
                            <DataGridTextColumn Header="Line Total"
                            Binding="{Binding LineTotal, StringFormat=C}"
                            IsReadOnly="True"
                            Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="Add Labor" Width="90" Margin="0,0,8,0" Command="{Binding AddLaborLineCommand}" />
                        <Button Content="Add Part" Width="90" Margin="0,0,8,0" Command="{Binding AddPartLineCommand}" />
                        <Button Content="Remove" Width="90" Command="{Binding RemoveLineCommand}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="{Binding CurrentWorkOrder.LaborTotal, StringFormat=Labor Total: {0:C}}" />
                        <TextBlock Text="{Binding CurrentWorkOrder.PartsTotal, StringFormat=Parts Total: {0:C}}" />
                        <TextBlock Text="{Binding CurrentWorkOrder.GrandTotal, StringFormat=Grand Total: {0:C}}" FontWeight="Bold" />
                        <TextBlock Text="{Binding CurrentWorkOrder.BalanceDue, StringFormat=Balance Due: {0:C}}" FontWeight="Bold" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save" Width="90" Margin="0,0,8,0" Command="{Binding SaveCommand}" />
                        <Button Content="Delete" Width="90" Margin="0,0,8,0" Command="{Binding DeleteCommand}" />
                        <Button Content="Clear" Width="90" Command="{Binding NewCommand}" />
                        <Button Content="Receipt" Width="90" Margin="0,0,8,0" Command="{Binding GenerateReceiptCommand}" />
                      <Button Content="Inspection Checklist"
                              Width="150"
                              Margin="0,0,8,0"
                              Command="{Binding OpenInspectionCommand}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>