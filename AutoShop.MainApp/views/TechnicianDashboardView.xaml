<UserControl x:Class="AutoShop.MainApp.Views.TechnicianDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                    <TextBlock Text="Technicians" FontSize="24" FontWeight="Bold" Margin="0,0,0,10" />
                    <Button Content="Refresh" Width="90" Command="{Binding RefreshCommand}" />
                </StackPanel>

                <ListBox ItemsSource="{Binding Technicians}"
                         SelectedItem="{Binding SelectedTechnician, Mode=TwoWay}"
                         DisplayMemberPath="FullName" />
            </DockPanel>
        </Border>

        <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                    <TextBlock Text="{Binding SelectedTechnician.FullName}" FontSize="24" FontWeight="Bold" />
                    <TextBlock Text="{Binding ActiveWorkOrders.Count, StringFormat=Active Jobs: {0}}" FontSize="14" />
                </StackPanel>

                <DataGrid ItemsSource="{Binding ActiveWorkOrders}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="WO #" Binding="{Binding WorkOrderNumber}" Width="*" />
                        <DataGridTextColumn Header="Customer" Binding="{Binding Customer.FullName}" Width="2*" />
                        <DataGridTextColumn Header="Vehicle" Binding="{Binding Vehicle.Make}" Width="*" />
                        <DataGridTextColumn Header="Model" Binding="{Binding Vehicle.Model}" Width="*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="*" />
                        <DataGridTextColumn Header="Total" Binding="{Binding GrandTotal, StringFormat=C}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>